@RULES.md

# This repository

This is the use-herdr source package. RULES.md is the shared rule set that
install.ps1 copies to <target>/.ai-swarm/RULES.md on every install.
Edit shared rules in RULES.md, not in a target project's copy.
